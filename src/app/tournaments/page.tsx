import { Container } from "react-bootstrap";
import styles from "./page.module.scss";

export default function Tournaments() {
  return (
    <main>
        <Container>
            <h2>Здесь будет страница с турнирами</h2>
        </Container>
    </main>
  );
}